# 开发笔记

> 用户向的安装和用法在 [README.md](README.md)。这份文档是给改代码的人看的：
> 本地测试台、站点真实 DOM 结构、数据结构与同步设计、发版流程，
> 以及一路踩过的坑。

## 目录

- [本地测试台](#本地测试台)
- [站点真实结构](#站点真实结构)
- [数据结构](#数据结构)
- [同步设计](#同步设计)
- [发版流程](#发版流程)
- [坚果云自查工具](#坚果云自查工具)
- [踩过的坑](#踩过的坑)

---

## 本地测试台

### 方式 A：自动测试台（不用连网、不碰真站）

```bash
cd fw-marks
python3 test/run-test.py
```

它会起一个本地服务，把 `test/pages/` 里那 14 个真实页面**按 URL 模式**伺服
（所以随便点 id 都能开，不会到处 404），注入脚本 + 一份假数据，
再用 headless Chrome 逐页跑、逐条断言。全绿就是没问题：

```
=== 书籍介绍 + 目录页 ===        === 正文页（一页多章）===
  ✅ 目录角标画出来了              ✅ 6 章都挂上了标记条
  ✅ 书名后挂了书籍标签            ✅ 每章都有 🔖 按钮
  ✅ 书名后显示书签数 🔖2          ✅ 点 🔖 真能加上书签
  ✅ 有「上次读到」提示条          ✅ 同位置再点 = 取消

=== 单章固定链接页 ===           === 收藏 / 首页 / 频道 / 索引 ===
  ✅ 只认出 1 章（回帖没被算进来） ✅ 书名后挂上了标签
131/131 通过
```

覆盖的页面：首页、收藏、文库、书籍介绍、纯目录列表、正文、单章固定链接、
主题索引、频道、动态、个人中心、标签云、某标签下的书。

它不只看「画对了没」，还会真的 `click()` 一下再读存储 —— 画得对但点不动照样会红。
另外专门盯着几个回归点：

- 点「上次读到」必须跳**章节固定链接**并自动复位，不能原地刷新
- 单章页只能认出 1 章（同页那 4 条回帖不许被当成章节）
- v1.0 留下的假数据（书 ID 被当成章 ID）必须被清掉，且不许误删真书
- 两个页面同时写存储，不许互相冲掉（`?fwtest=crosstab`，见
  [存储层的坑之二](#存储层的坑之二整包覆盖会互相冲掉v111-修)）
- 查新章只能手动触发，不许挂后台定时器（静态检查 + `?fwtest=upd`）

`?fwtest=<mode>` 是给断言脚本切场景用的：`strip` / `landing` / `scroll` / `legacy` /
`dupbmk` / `progcleared` / `ratecleared` / `upd` / `crosstab`。

想自己用眼睛看：`python3 test/run-test.py --serve`，浏览器开 <http://127.0.0.1:8899/>，
可点的地址都列在 <http://127.0.0.1:8899/__routes__>。
碰到没抓夹具的页面会给张说明页（不是 404），照着往 `PATTERNS` 加一行就行。

> 夹具的样式表指向站点 CDN，本地没代理会加载不出来 —— 页面很朴素，但功能照跑。

两个踩过的坑，改脚本时别踩回去：

- Chrome 必须用 **`--headless=old`**。新版 `--headless=new` 是个真 app 进程，
  会在 Dock 里注册图标一直跳；旧版是纯后台进程，抓 DOM 能力完全一样
- 每次启 Chrome 都要**独立的 `--user-data-dir`**。共用一个的话，上一个还没退干净时
  新启的会挂到那个实例上开标签页，`--dump-dom` 一个字都不输出 ——
  表现就是「单独跑能过、连着跑就挂」

### 方式 B：在真站上试，零安装

Safari → 设置 → 高级 → 勾「显示网页开发者功能」→ 打开废文网任意页面 →
**开发 → 显示 JavaScript 控制台** → 把 `fw-marks.user.js` 全文粘进去回车。
网站顶栏立刻多出一项 **📑 我的标记**。

脚本检测不到扩展的 GM API 时会自动降级用 `localStorage`，所以裸控制台里也能跑完整功能
（只有跨域的坚果云同步会被浏览器 CORS 拦住，GitHub API 不受影响）。
缺点是刷新就没了，适合快速验证。

---

## 站点真实结构

这一节是**对着线上 DOM 逐条校验过的**，不是猜的。脚本里的选择器全集中在
`FW.SEL` 一处，改版只要动那一块。

### 页面类型（靠 URL 判，比猜 DOM 稳）

| URL | 是什么 |
|---|---|
| `/threads/{tid}/profile` | 书籍介绍 + 目录 |
| `/threads/{tid}/chapter_index` | 纯目录列表（**另一个入口，结构同上**） |
| `/threads/{tid}?page=N` | 正文页，**一页塞好几章** |
| `/posts/{pid}` | 单章固定链接（下面还跟着该章的回帖） |
| `/books`、`/collection/{uid}`、`/thread_index`、`/channels/{n}` | 书列表 |

**书 ID = threadId，章 ID = postId。**

### 正文页：一页多章，还混着回帖

```
div#post{pid}
└ div.panel.panel-default#postbody{pid}
  ├ div.panel-heading                  作者 / 时间 / #序号
  └ div.panel-body.post-body
    └ div.main-text.indentation
      ├ div.text-center > strong > a[href="/posts/{pid}"]   ← 只有章节号！
      └ span#full{pid}                                       ← 正文
```

- 同一页里**回帖也长成 `div#post{pid}`**，区别是**没有标题行** —— 据此剔掉。
  实测某本书一页 10 个 post 块里只有 6 个是章节
- 正文页标题区**只有章节号**（「2」），章节名不在这儿

### 单章页 `/posts/{pid}`：另一种排版

```
div.panel#post{pid}
└ div.panel-body.text-center
  ├ div.text-center > strong.h3    章节号
  └ div.text-center > strong.h5    章节名
```

没有 `<a>`，所以正文页那套选择器在这里抓不到。而且同页还跟着 4 条回帖、
它们也都有 `.main-text` 和 `#full{id}` —— 所以**只认 URL 指的那一条**。

### 目录：手机版和桌面版是两套 DOM

```
div.hidden-sm.hidden-md.hidden-lg     手机版：一排 a.btn.btn-info.btn-block
div.hidden-xs.table-hover > table     桌面版：每行 5 个单元格
                                        第1格 章节号 / 第2格 章节名 / 字数 / 时间 / …
```

两套同时存在于 DOM 里，靠网站自己的响应式决定显示哪个 —— 所以**两套都要画角标**。
桌面版一行里有**两个链接指向同一章**，只在最后那个上挂角标，免得画两遍。

**章节名的权威来源是目录**（桌面版第 2 格）。正文页只有章节号，所以脚本在
逛目录时把完整名字学下来、回填给进度和书签。

### 书列表：class 命名有四种

| 页面 | `<article class="…">` |
|---|---|
| `/books` | `item1id{tid}` |
| `/collection/{uid}` | `thread{tid}` |
| `/channels/{n}` | `threadid{tid}` |
| `/thread_index` | `threadid{tid}` 和 `thread{n} brief-{n}` |

认 class 必然漏。所以**改成认「书名链接」**：凡是指向 `/threads/{n}` 或
`/threads/{n}/profile` 的链接就挂角标，一招通吃。带 `?query` 的跳过 ——
那是排序/筛选链接，不是书名。

> 站点有的 `href` **尾部带空格**（`".../profile "`），匹配前记得 `trim()`。

### 镜像域名

`废文网.com`（`xn--pxtr7m5ny.com`）、`废文.com/.net/.link`（`xn--pxtr7m`）、
`sosad.fun`、`sosadfun.com` —— `@match` 里都写上了。

### 反爬

站点在 **AWS WAF** 后面。首次访问会返回 `202` + `x-amzn-waf-action: challenge`，
要浏览器跑 JS 拿到 `aws-waf-token` cookie 才放行。命令行抓不到正文；
headless 浏览器会被判定成机器人（403）。想抓夹具只能借真实浏览器的 cookie。

---

## 数据结构

### 本地存储

```
COLLECTIONS = ['books', 'chaps', 'bmks', 'prog']
```

所有可同步的表都长同一个样子：**扁平 map + 字符串 key，每条记录带 `updatedAt`，
删除是软删（`deleted: true`）**。合并逻辑因此只需要一份。
**加新表只改这一处** —— `load` / `snapshot` / `apply` 三处会自动跟上。

| 表 | 内容 |
|---|---|
| `books[书id]` | 书名、自定义标签、书评、章节总数/完结状态 |
| `prog[书id]` | 阅读进度：读到哪章、章内位置、定位锚文本 |
| `chaps[书id\|章id]` | 章节标记（精彩/一般/跳过）。`note` 是 v1.10 及以前写的备注，现在只读不写 |
| `bmks[书id\|章id-时间戳]` | 书签。**章级**：只记是哪一章，不记章内位置（v1.11 起） |
| `tagPool` | 用过的自定义标签 |

**为什么进度要单独一张表**：它塞在书记录里的时候，一边读一边刷
`book.updatedAt`。而合并是「整条记录取最新」，于是另一台设备刚加的标签/书评，
会被进度带起来的那次更新整条盖掉。拆开就互不干扰了。

### 迁移链

```js
const steps = [['mig1','_mig1'], ['mig2','_mig2'], ['mig3','_mig3'],
               ['mig4','_mig4'], ['mig5','_mig5']];
```

一条一条叠加、各有独立完成标记，以后改结构就加 `_mig6` 并补一行。顺序有意义。

| 步 | 做什么 |
|---|---|
| `_mig1` | 清掉 v1.0 猜错 URL 规律留下的假记录（见下） |
| `_mig2` | 进度从书记录搬进 `prog` 表 |
| `_mig3` | 收掉历史上已同步出来的重复书签 |
| `_mig4` | 同步配置从「文件路径」推导出「文件夹」 |
| `_mig5` | 书签改章级后，同一章的多条按新规则再收一次 |

### 存储层的坑：GM 存储是异步的

`GM.setValue` 要过扩展的消息通道，**页面关掉那一刻发出去的写入基本收不到** ——
「读到一半关掉、进度没更新」就是这么丢的。

所以 `GMx.set` 先**同步**写一份 `localStorage` 镜像（在 `await` 之前，调用即落盘），
再去写 GM；`GMx.get` 两份都读、按 `savedAt` 取新的那份。GM 那次写丢了，
下次打开会自动捡起镜像并写回，自己就修好了。

### 存储层的坑之二：整包覆盖会互相冲掉（v1.11 修）

每个页面都有自己那一份内存里的 `DB.d`。原来写回去是**整包覆盖**，谁最后写谁赢。
真实症状：

- 详情页点进某章读 → **返回**详情页。Safari 的「返回」是**页面缓存**，恢复出来的
  是原封不动的老页面，连它内存里那份 `DB.d` 都是当初加载时的 —— 它一存（比如
  `pagehide` 的兜底 flush），刚记的进度就被冲回去了
- 从列表页再点进去，看到的还是列表页当初加载时的旧进度

两头都得治：

| | 怎么做 | 代码 |
|---|---|---|
| 写 | 写之前先把存储里那份读出来，**逐条比 `updatedAt` 合并**，再写 | `DB._flush()` → `DB.mergeStored(DB.stored())` |
| 读/显示 | 回到这个页面时主动拉一次，然后重绘 | `pullStore()`，挂在 `pageshow(persisted)` / `visibilitychange` / `storage` 三个事件上 |

读用的是**同步的** `localStorage` 镜像 —— GM 的读是异步的，而「页面正在被关掉」
那一刻等不起异步。合并规则和云同步那套完全一样，所以行为一致、也一样收敛。

`cfg` / `status` / `site` 这些不是「一条条的记录」，没有自己的 `updatedAt`，
只能整块取新的那份，用 `savedAt` 判。注意 `_flush()` 里是**先**把自己的 `savedAt`
盖成现在、**再**合并 —— 所以保存时永远是内存这份赢，不会把用户刚改的设置冲掉；
只有 `pullStore()` 这种「主动去拉」的场合才会采纳存储那份。

> 回归测试：`?fwtest=crosstab`（直接改存储假装另一个标签页写过，再触发本页写入），
> 外加一条静态检查盯住 `_flush()` 里必须有 `mergeStored`。

---

## 同步设计

### 云端存成什么样

配置里填的是**文件夹**，里面放哪些文件由脚本自己管 —— **一个表一个文件**：

```
<你的文件夹>/
  books.json    书名、标签、书评
  prog.json     阅读进度
  chaps.json    章节标记
  bmks.json     书签
  meta.json     标签池等
```

好处是**只推改过的那个文件**：只翻了几页书就只重写 `prog.json`，别的一个字节不动
（坚果云免费版每月才 1GB 上传）。文件清单从 `COLLECTIONS` 推导，所以加表不用改同步逻辑。

> 为了让「没变就不推」真的生效，序列化时会**按键名排序** —— 否则两台设备合并出来的
> key 顺序不同，内容一样也会被判成「变了」，白推一次。

从 v1.7 及以前升上来的老数据（单个 `fw-marks.json`）会在首次同步时被**读一次并合并进来**，
不用手动搬。老文件保留不动，当个额外备份。

### 冲突怎么合并

同步一轮：**两个云端都拉 → 和本地逐条比 `updatedAt` 取最新 → 合并结果推回两边**。

- 粒度是**每条记录**（每本书 / 每章 / 每个书签 / 每本书的进度），不是整个文件
- 手机标了 A 章、Mac 标了 B 章 → 合并后**两个都在**
- 哪个云端落后，推送时自动补齐；一个挂了另一个照常用
- 删除一律**软删**（`deleted` + 时间戳），否则同步会把删掉的东西复活

**书签跨设备去重**：书签的 key 带时间戳，所以同一个位置在两台设备上各加一次，
key 不一样、合并后会并排留下来。合并完会跑一次去重：**同书同章就算重复**
（书签是章级的，v1.11 起不再比章内位置）。留哪条**完全由数据决定**
（按 `createdAt`、再按 key 排序取第一条）—— 必须如此，
否则两台设备各留各的、互相删来删去永远不收敛。

> 老数据身上的 `cpct` / `anchor` 留着没删（删是不可逆的、还会同步出去），
> 但跳转和显示都不再看它们 —— 见 `data-bmkgo` 的处理：只取 `{bid, cid, title, url}`。
> 否则新旧书签的跳转行为会不一样。

**已知限制**：同一条记录被两台设备改了不同字段（比如手机改标签、Mac 改书评），
是**整条取最新**，不是逐字段合并 —— 另一边那次改动会丢。

> 注意进度里带着**约 40 字定位锚文本**（会暴露你读了什么），
> 所以两个云端都选了鉴权存储，别放公共仓库。

---

## 发版流程

发新版时两个文件**必须一起传** —— `.meta.js` 是给扩展比版本号用的，
只传 `.user.js` 它不知道有新版：

```bash
cd ~/fw-marks
# 改完代码后：升 @version → 重新生成 meta → 提交推送
python3 -c "import re;s=open('fw-marks.user.js',encoding='utf-8').read();\
open('fw-marks.meta.js','w',encoding='utf-8').write(\
re.search(r'// ==UserScript==.*?// ==/UserScript==',s,re.S).group(0)+'\n')"
git add -A && git commit -m "vX.Y.Z" && git push
```

发版前跑一遍测试台（含源码级的凭据泄漏检查）：

```bash
python3 test/run-test.py
```

---

## 坚果云自查工具

同步失败只给得出一个状态码时，用它把该问的都问一遍：

```bash
python3 test/dav-check.py
```

1. 列出账号下**真实存在**的顶层同步文件夹（地址里文件夹名写错是头号原因）
2. 试读目标文件
3. 征得同意后写一个**另取名字**的测试文件再删掉，验证写权限

密码用 `getpass` 读：不回显、不作为命令行参数、不写进任何文件。

> 现在脚本里也内置了 **🔍 测试连接**，一般不需要跑这个外部工具了。

---

## 踩过的坑

### 一次性数据修复（v1.0 → v1.2.1）

v1.0 靠启发式猜 URL 规律，在这个站上猜错了：**书籍介绍页被当成正文页**，
于是「书 ID」和「章 ID」被解析成了同一个数字。后果就是
`progress.cid == 书 ID` → 点「继续读」跳回书籍主页自己 = 原地刷新。

v1.2.1 首次运行会自动认出这类记录（章 ID 不可能等于书 ID）并软删掉，
顺手把纯空壳的假书条目也清掉 —— 但**有标记、有书签、有标签或有书评的真书一律不动**。
清理会同步到云端，所以另一台设备上也不会再冒出来。

---

### 其他

- **`.gitignore` 只对「还没被跟踪」的文件生效**。已经进了跟踪列表的文件
  （比如被 `git add -A` 带进去的 `.idea/.gitignore`），后加的忽略规则管不住它，
  得 `git rm --cached` 从索引里摘出来
- **`git push` 到 GitHub 需要走代理**（直连 443 会超时），但 `api.github.com`
  和 `raw.githubusercontent.com` **直连反而更快**。所以代理只配在仓库级：
  `git config --local http.proxy …`，不碰全局配置
- **别在家目录根上 `git init`**；公司的 `init.templateDir` 会把钩子拷进个人仓库，
  用 `git init --template=` 绕开
- **提交身份用仓库级覆盖**，免得工作邮箱进公共仓库的历史
