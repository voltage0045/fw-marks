# ---- 绝对不能进公共仓库的东西 ----
#
# test/pages/ 是从登录态抓的真实页面快照，里面有：
#   · collection.html = 自己的收藏夹（30 本书名）
#   · 各页合计 24 个他人用户 id
#   · 912K 别人网站的页面内容（不该由我们转发布）
# 测试台要用它们，但它们只留在本机。
# 需要重新生成时：登录后抓页面 → 把超过 60 字的文本节点换成占位符 → 存进 test/pages/
test/pages/

# 打包给手机用的临时产物
*.zip

# 系统垃圾
.DS_Store
